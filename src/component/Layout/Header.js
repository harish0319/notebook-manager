import Form from "../Input/Form";

const Header = () => {
    return (
        <header className="header">
            <h1>Note Book Manager</h1>
            <Form />
        </header>
    );
};

export default Header;